#include <stdio.h>

/**
 * main - Task Seven
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	int num;

	for (num = 0; num < 10; num++)
	putchar(num + '0');
	if (number < 9)
	{
		putchar(' ');
	}
	putchar('\n');
	return (0);
}
